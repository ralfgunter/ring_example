mainmodule ring {
    readonly CProxy_Main mainProxy;

    mainchare Main {
        entry Main(CkArgMsg *m);
        entry [reductiontarget] void cycleDone();
    }

    array [1D] RingArc {
        entry RingArc(int numSlices, int maxNumParticlesPerSlice);
        entry void passMsg(int srcIndex, int otherNumParticles, Particle ps[otherNumParticles], double forces[otherNumParticles]);
        entry void startCycle();
    }
}
